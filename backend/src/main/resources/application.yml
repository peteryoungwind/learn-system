server:
  port: 8080

spring:
  application:
    name: learn-system-backend
  datasource:
    url: ${DB_URL:jdbc:mysql://139.196.43.133:3306/learn_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:pzq18217074393}
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    baseline-on-migrate: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

app:
  jwt:
    secret: ${JWT_SECRET:01234567890123456789012345678901}
    expiration-seconds: ${JWT_EXPIRATION_SECONDS:604800}
  upload:
    temp-dir: ${UPLOAD_TEMP_DIR:${java.io.tmpdir}/learn-system}
  preview:
    libreoffice-command: ${LIBREOFFICE_COMMAND:soffice}
